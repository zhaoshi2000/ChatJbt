# v1.2.0

从 v1.1.0 单账号 / 单工作标签页架构升级：

- 新增账号工作区、专属令牌、管理员创建/重置，后端按账号鉴权。
- 新增会话模型，账号 → 会话 → 任务的持久归属；同会话只允许一个活动任务。
- 扩展每个会话独立 lane、tab、documentKey、lease、outbox；移除单全局 active/bridge 状态。
- 不同登录账号使用独立浏览器用户数据目录；A/B/自定义编号启动脚本。
- 网页独立 URL 会话选择、独立 sessionStorage 草稿/待确认发送、同会话双观察者、Markdown 子集渲染、重命名/删除/导出。
- 首次配对流程改为先创建账号；旧 local-token.txt 是管理员令牌，不再直接用于聊天。
- 新会话不接管已有上游对话；记录 /c 地址后锁定，无法确认提交结果不盲目重发。
- 旧版无账号字段的记录只读归档，不猜测归属；不把旧进行中的任务交给新账号。
- 保留无侧边栏、Java 21、端口 48643、任务恢复、搜索等待判断和安全停止脚本。
- 替换旧测试用例与旧报告为本次实际执行记录，不沿用旧版成功数量。
- 聊天页新增图片粘贴/选择，扩展可上传图片，并可采集、持久化和下载 ChatGPT 生成图片。
- 新增每条任务的模型选择：保持当前、5.6 即时、5.6 思考、5.6 Pro、6 Pro；扩展通过可见模型菜单切换并核验，不调用 ChatGPT 私有接口。
- 发送时自动创建、唤醒并等待 ChatGPT 工作标签页就绪；仅在登录失效或超时时要求人工介入。
- 兼容新版 ChatGPT 将生成图片卡片放在回复节点外的页面结构；按当前消息区间识别 OpenAI 图片地址，避免任务在图片已生成后仍持续等待。
- 首次发送会持续等待冷启动或休眠中的 ChatGPT 工作页就绪，不再因网页加载超过 20 秒而要求再点一次发送。
- 发送改为先入队再异步唤醒工作页，首次点击立即生效；同一账号只复用一个 ChatGPT 工作标签页，不同本地会话串行切换。
