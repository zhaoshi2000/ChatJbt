# 逗包 v1.2.0：多网页 / 多账号，先看这里

仍是独立网页，不使用浏览器侧边栏。默认地址 `http://127.0.0.1:48643/web/`，需要 Java 21 或以上。包内已编译 `out/backend.jar`，正常运行不需要 Node、Python、MySQL、Redis。

## 先停旧版，再升级

1. 等任务结束，运行旧项目 `stop-all.bat` 或 `stop-backend.bat`。禁用旧扩展，不能让 1.1 与 1.2 同时桥接。
2. 将本包解压到新文件夹。需要保留旧数据时，**先备份旧 data 文件夹**，在新版首次启动前复制整个 data 文件夹过来。只复制 data，不复制旧 out / extension / config。v1.2 会升级数据格式，回退旧版时使用升级前备份。
3. 双击 `run-doubao-web.bat` 启动。首次使用时在聊天页点“连接账号”，再点“生成并复制令牌”；不需要先查找 `data/local-token.txt`。也可以直接双击下一节的 A 窗口脚本，它会先检查并启动后端。

## 两个不同账号：分别设置 A、B 窗口

| 用途 | 双击哪个文件 |
|---|---|
| 打开账号 A 独立窗口 | `open-account-A.bat` |
| 打开账号 B 独立窗口 | `open-account-B.bat` |
| 再增加 C、D 等窗口 | `open-account-window.bat`，输入 C、D 等编号 |

脚本默认寻找 Edge，找不到再尝试 Chrome。每个编号使用独立用户数据目录；再次打开相同编号会复用该编号的登录数据。**只打开普通新标签页不能得到不同账号的登录隔离。**

首次可在 A/B 窗口各输入 `edge://version`（Chrome 为 `chrome://version`），查看“个人资料路径 / Profile Path”确认目录分别是 edge-a、edge-b。若企业策略强制相同目录，脚本会报错，请联系管理员，不要继续把两个账号填进同一配置文件。

### 账号 A

1. 双击 `open-account-A.bat`。
2. 在这个新窗口的扩展管理页（Edge 输入 `edge://extensions`，Chrome 输入 `chrome://extensions`）开启开发人员模式，加载新项目的 **extension 文件夹**。名称为“逗包 · 多账号后台桥接”，版本 **1.2.0**。刷新逗包网页。
3. 回到逗包，点 **连接账号**。如果还没有任何账号，输入名称“账号 A”，点 **生成并复制令牌**；令牌会直接显示在聊天页并复制到剪贴板。
4. 勾选“此浏览器配置文件仅用于当前登录账号”，点 **保存并配对**。也可以把聊天页中的 **账号 A 专属令牌** 粘贴到扩展的 GPT 桥接设置中。
5. 关闭设置后直接在逗包提问。程序会自动创建、唤醒并等待 ChatGPT 工作页；首次尚未登录时才会聚焦该页，请登录后重试。不要关闭正在生成任务的工作标签页。

### 账号 B

双击 `open-account-B.bat`，在 B 窗口也加载同一个 extension 文件夹；重复上述流程，在账号管理里创建“账号 B”，使用 **B 的专属令牌**，在 B 的 ChatGPT 工作页登录 B。不要将 A 的专属令牌填到 B 窗口。

**管理员令牌与账号令牌不能混用：**

- `data/local-token.txt`：本地管理员令牌，只用于创建/管理账号、停后端。不是 ChatGPT 密码或 API Key。
- 账号专属令牌：创建账号后显示，用于该账号网页和扩展。只显示一次，关闭窗口前保存；丢失可在账号管理重置。令牌重置不删除历史，但会解除旧浏览器绑定，并使旧令牌失效。

## 同一个账号多开网页

在这个账号的逗包网页点击 **多开一个网页**，再点击 **新对话**。每个本地会话对应自己的 ChatGPT 工作标签页。点击 **另页查看** 是查看同一条会话，不是新建独立上下文。

同账号默认最多 3 个不同会话同时执行，多出的按队列等待。两个网页对同一个会话同时发送时，后端只接受一个正在生成的任务。不同账号的队列分别处理。

## 不要误用

- 账号 A/B 是本地标签，不会替你申请 ChatGPT 账号，也不会自动确认当前登录邮箱。请手动确认真实登录身份，运行期间不要在已绑定的配置文件里切换上游账号。
- 这是不同账号、会话的路由和存储访问隔离；**不是隐藏于本机管理员的加密空间**。管理员令牌可管理所有本地账号，data 中的聊天记录为明文。
- 独立浏览器目录位于 `%LOCALAPPDATA%\Doubao\BrowserProfiles`，不是项目目录。不要开启资料同步、复制别的账号浏览器目录，或删除它们后期待保留登录。
- 旧版历史无法确认归属，归入只读旧版归档；在账号管理中 **导出旧版记录**，不会自动混入新账号。
- 不需侧边栏，但网页桥接仍需保留对应已登录的工作标签页。电脑休眠、浏览器退出、上游界面变更可能中断任务；无法确认提交结果时不会自动重复发消息。

本次完成 Linux Java / HTTP、Node 模拟后台、离线 Chromium 受控界面测试。真实 Windows 启动脚本、扩展安装、两个真实登录账号并行仍需真机验收，详见 `docs/TEST_REPORT.md`。
